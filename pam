#!/usr/bin/env bash
set -euo pipefail

repo_dir=$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)
executable="$repo_dir/.venv/bin/pam-discord"

if [[ ! -x "$executable" ]]; then
  echo "Pam is not installed yet. Run: ./install.sh" >&2
  exit 1
fi

exec "$executable" "$@"
